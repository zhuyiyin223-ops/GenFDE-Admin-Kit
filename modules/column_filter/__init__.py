"""系统能力：表格列头筛选。可被业务域引用。

字符串列弹出关键词模糊搜索与空值勾选，多个关键词空格分隔按 OR 命中；日期列弹出日期范围、快捷单选与空值；数字列弹出闭区间与空值，缺一端则为 ≥ 或 ≤；选择列（布尔与枚举同一套）下拉多选与空值勾选。
关联展示列声明 relation，或用 related_text_spec 声明逻辑外键展示列；
交互复用字符串弹窗，按中间表/从表反查主表，空值表示无关联行。
表格上方用芯片展示生效条件。
"""
